# shopify-collection-filters
Custom Shopify collection page filters built using Shopify native filtering system — no apps required. Supports price, variant-based filters (color, size), and AJAX updates for fast, seamless UX.
